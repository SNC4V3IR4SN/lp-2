let idade: number = 25

let nome: string 
nome = "gabriel"

let ativo: boolean = true 

let notas: number[] = [1, 2, 3, 4]

let pessoas : [string, number] = ["joao", 20]

let valor : any ="pode receber qualque coisa"

let doisTipos : number | string = "ABCD"

function soma(num1: number, num2: number) :number {
  return num1 + num2
}